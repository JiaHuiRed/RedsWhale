# RedsWhale 环境变量配置
# Shell 导出的变量会覆盖此文件中的值。
# 将此文件复制为 `.env`，然后取消注释你需要的变量。

# DeepSeek API（默认提供商）
# 从 DeepSeek 获取 API key，保存在本地 `.env` 中。
# DEEPSEEK_API_KEY=

# DeepSeek 官方平台地址（详见 api-docs.deepseek.com）；`deepseek-cn` 使用相同地址。
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_PROVIDER=deepseek-cn

# V4 模型选择。兼容别名 `deepseek-chat` 会在 TUI 中自动映射到当前 V4 Flash 模型。
# DEEPSEEK_MODEL=deepseek-v4-pro
# DEEPSEEK_MODEL=deepseek-v4-flash

# NVIDIA NIM 托管的 DeepSeek V4
# 通过 NVIDIA 的 OpenAI 兼容端点路由时使用此提供商。
# DEEPSEEK_PROVIDER=nvidia-nim
# NVIDIA_API_KEY=
# NVIDIA_NIM_API_KEY=
# NVIDIA_NIM_BASE_URL=https://integrate.api.nvidia.com/v1
# NIM_BASE_URL=https://integrate.api.nvidia.com/v1
# NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
# NVIDIA_NIM_MODEL=deepseek-ai/deepseek-v4-pro

# 日志
# `DEEPSEEK_LOG_LEVEL` 由 CLI 传递；`RUST_LOG` 启用 TUI 的详细日志（info/debug/trace）。
# DEEPSEEK_LOG_LEVEL=debug
# RUST_LOG=deepseek_tui=debug

# 智能体安全默认值
# `on-request` 在高风险操作前询问；`workspace-write` 限制写入在工作区内。
# DEEPSEEK_APPROVAL_POLICY=on-request
# DEEPSEEK_SANDBOX_MODE=workspace-write
# DEEPSEEK_ALLOW_SHELL=true
# DEEPSEEK_YOLO=true

# 可选扩展路径
# DEEPSEEK_SKILLS_DIR=~/.deepseek/skills
# DEEPSEEK_MCP_CONFIG=~/.deepseek/mcp.json

# **English**: [.env.example.en](.env.example.en)
